{
  "name": "@waggora/workspace",
  "version": "1.0.0",
  "description": "Waggora monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:setup": "cd packages/db && npm run setup",
    "db:migrate": "cd packages/db && npm run migrate:dev",
    "db:push": "cd packages/db && npm run db:push",
    "db:studio": "cd packages/db && npm run studio",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
